[package]
name = "rusty"
version = "0.1.0"
authors = []

[lib]
name = "rusty"
path = "src/lib.rs"
crate-type = ["dylib"]

[dependencies]
rustler = { git = "https://github.com/rusterlium/rustler" }
rustler_codegen = { git = "https://github.com/rusterlium/rustler" }
